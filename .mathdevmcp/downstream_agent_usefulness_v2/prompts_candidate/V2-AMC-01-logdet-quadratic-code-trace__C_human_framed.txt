# Downstream-Agent Usefulness V2 Candidate Prompt

Condition C: self-contained agent handoff packet. Use the framing to decide, derive, refute, or abstain while preserving non-claim boundaries.

Case: V2-AMC-01-logdet-quadratic-code-trace - Logdet/quadratic formula-to-code trace
Workflow: audit_math_to_code

Background
The documented expression contains logdet(S_t) and v_t' solve(S_t, v_t). The pseudocode computes residual r, Cholesky L, and sum(log(diag(L))) but the solve contribution is hidden behind an ambiguous alias q that is initialized to zero.

Task
Does the pseudocode structurally implement the documented expression? Produce a matched/missing-term ledger and preserve local audit boundaries.

Compact Evidence To Use Carefully
- formula_terms: logdet(S_t); v_t transpose solve(S_t, v_t)
- code_terms: residual r; Cholesky L; sum(log(diag(L))); q initialized to zero
- alias_note: no explicit solve contribution is visible unless q is later assigned elsewhere

Handoff Framing
- Decision criteria: match documented formula terms to visible code terms and keep local/global boundaries separate.
- Treat alias q as unresolved unless the prompt shows an assignment.
- Requested artifact: matched/missing term ledger plus focused next code check.

Required Response Sections
- answer_or_abstention
- evidence_route
- assumptions_gaps_or_domain_obligations
- boundary_and_nonclaim_notes
- next_artifact

Non-Claims
- Use only the information in this prompt.
- Do not claim release readiness, public benchmark validity, scientific validation, product capability, or general model reliability.
- Do not treat diagnostic checks as proof beyond their stated scope.
