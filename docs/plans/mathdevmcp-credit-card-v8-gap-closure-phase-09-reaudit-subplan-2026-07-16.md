# Phase 09 Subplan: Exact v8 Re-Audit And Closeout

## Phase Objective

Repeat the original v8 MCP application audit against the frozen source and
record the remaining engineering and scientific gaps.

## Entry Conditions Inherited From Previous Phase

- Phases 01-08 have result records and their vetoes pass or are explicitly
  carried as unresolved blockers.
- Publication remains disabled and v8 remains unchanged.

## Required Artifacts

- Fresh raw evidence and eight regenerated Markdown reports from the exact
  harness.
- Before/after gap matrix and 65-tool applicability accounting.
- Full CPU-only test manifest and source digest verification.
- `mathdevmcp-credit-card-v8-gap-closure-phase-09-result-2026-07-16.md`.
- `mathdevmcp-credit-card-v8-gap-closure-result-2026-07-16.md`.

## Required Checks

- `CUDA_VISIBLE_DEVICES=-1 python scripts/run_credit_card_v8_mcp_audit.py`.
- Focused parity/accounting assertions and full CPU-only `pytest` suite.
- `git diff --check`, compile checks, source digest, generated Markdown
  nonempty, raw manifest accounting, and CLI/FastMCP parity.
- Final critical inspection of every regenerated report, not status counts
  alone.

## Evidence Contract

- Pass: exact comparator is rerun and every closed/remaining gap is tied to
  reproducible evidence.
- Veto: changed source, missing tool accounting, parity drift, status inflation,
  unsupported scientific claim, or publication change.
- Non-claim: even the strongest completion decision is bounded to this v8 lane.

## Forbidden Claims And Actions

- Do not call the whole document correct or publication-ready.
- Do not hide remaining scientific unknowns behind engineering success.
- Do not commit or push unless separately requested.

## Exact Completion Conditions

Choose exactly one master-program completion decision and record why every
stronger decision is not justified.

## Stop Conditions

Stop for user direction only if the evidence requires source editing,
publication/release authority, a new dependency/external service, or a material
scientific interpretation choice. Otherwise record an honest bounded result.

## Skeptical Pre-Run Audit

`PASS WITH HARNESS CLARIFICATION`

- Baseline/comparator: the same reviewed
  `scripts/run_credit_card_v8_mcp_audit.py` harness and the frozen v8 source
  digest are used; the source digest is checked before and after the run.
- Promotion criteria: packet and tree byte counts are product-usability checks
  only.  They do not promote mathematical or scientific claims.
- Vetoes: changed source bytes, missing tool accounting, public-surface parity
  drift, publication enablement, unsupported claim inflation, resolver failure,
  or failing tests veto a bounded success decision.
- Environment: scientific/test paths are run with
  `CUDA_VISIBLE_DEVICES=-1`; no GPU conclusion is intended.
- Artifacts: the harness regenerates seven native Markdown reports and raw
  evidence.  The eighth critical-gap analysis is not regenerated by the
  harness and must be refreshed separately from the new evidence before
  closeout; its pre-existing bytes are not accepted as fresh evidence.
- Stop conditions: the run remains local, reversible, and non-publishing.  No
  missing human authority is needed to execute it.
- Pre-mortem: a command could succeed while leaving stale reports, measuring
  only status counts, or hiding resolver/parity failures.  The post-run audit
  therefore checks report mtimes/content, raw packet ledgers, exact sizes,
  source digest, resolver behavior, and report substance before deciding.

## Post-Run Contradiction Repair Gate

The first closeout inspection found a material aggregate-report contradiction:
all nine selected targets carry verified evidence schema `1.0` bindings, while
the target and document promotion summaries still use legacy prose claiming
that exact Phase 01 binding is absent. This is an engineering presentation
defect, not evidence against the mathematical targets. It must be repaired
before a bounded-success decision.

The repair is limited to deriving target/document promotion explanations and
branch-ranking boundaries from the already verified integrity state. Legacy or
failed target paths must retain the legacy binding veto. Current binding still
cannot enable claim promotion or publication.

Focused repair checks:

- current v8 target, aggregate audit, compact response, and ranking summaries
  contain no claim that exact binding is absent;
- current promotion errors contain only the publication quarantine veto;
- a genuinely unbound/failure path retains
  `legacy_unbound_document_evidence`;
- `can_promote` and `publication_enabled` remain false;
- exact comparator, transport parity, source digest, compile checks, and the
  affected focused suites pass again.
