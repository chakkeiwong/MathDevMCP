# Phase 02 Independent Plan Review Round 4 Bundle

Date: 2026-07-11

Review name: `mathdevmcp-real-document-remediation-p02-plan-r4`

Supervisor/executor: Codex

Reviewer: fresh independent Codex read-only reviewer; external Claude transport
is policy-denied, so no content is sent externally

## Role Boundary

READ-ONLY REVIEW ONLY. Bounded local inspection and digest/reconstruction
commands are permitted. Do not edit files, run Phase 02 implementation or
mathematical backends, launch agents, use network/external services, or change
state. The local LaTeXML/Pandoc version commands have already been measured;
do not run parser fidelity jobs. Do not authorize publication, source edits,
Phase 03, or a human, runtime, product, funding, model-file, or
scientific-claim boundary.

## Objective

Determine whether the repaired Phase 02 subplan and its compact plus complete
materialized extraction oracles are consistent, correct, feasible,
deterministic, fail-closed, and complete enough to implement without choosing
identity-bearing fields or expected outputs after observing implementation.

## Required Artifacts

- `docs/plans/mathdevmcp-real-document-remediation-phase-02-label-scoped-extraction-subplan-2026-07-11.md`;
- `docs/plans/mathdevmcp-real-document-remediation-phase-02-extraction-oracle-2026-07-11.json`;
- `docs/plans/mathdevmcp-real-document-remediation-phase-02-materialized-obligations-oracle-2026-07-11.json`;
- `docs/reviews/mathdevmcp-real-document-remediation-phase-02-plan-review-r1-result-2026-07-11.md`;
- `docs/reviews/mathdevmcp-real-document-remediation-phase-02-plan-review-r2-result-2026-07-11.md`;
- `docs/reviews/mathdevmcp-real-document-remediation-phase-02-plan-review-r3-result-2026-07-11.md`;
- exact reviewed fixtures, two frozen source files, and sealed P01 predecessor
  pair named by the plan;
- relevant current locator/parser/governance code only where necessary to
  assess feasibility and boundary safety.

Do not broaden into Phase 02 implementation or Phase 03 semantics.

## Round 3 Findings And Claimed Repairs

Round 3 returned `REVISE` with two material blockers. Its durable result record
above is authoritative. The current bytes claim these visible repairs:

1. Row localization/classification/grouping is now closed by machine-readable
   `environment_locator`, `row_classifier`, and `grouping_grammar` registries.
   They define exact environment/row boundaries, comment and brace rules,
   lexical shape precedence, finite transitions, nested-aligned exception,
   stop/overlap policy, diagnostic states, and reason assignment. No shape
   depends on ownership. A `target_normalizer` registry separately fixes the
   exact owned-row-to-members/display transformation.
2. A second immutable oracle contains all 17 complete strict identity payloads,
   canonical byte counts, obligation digests, and obligation ids. The compact
   oracle binds its exact SHA-256 and defines independent source-byte
   materialization. Document ids, environments/parser tokens, row ids/indexes,
   full span objects, source math, normalized targets, inventories,
   diagnostics, provenance, and derived identities are therefore predeclared.

The repairs also add a fixed extraction-only parser fidelity profile: exact
local executables/versions, version/fidelity argv templates, 13-source
allowlist, result-round-only outputs, minimal environment, timeouts, raw
artifacts, lexicographic seven-field exact-fidelity rubric, and fail-closed
vetoes. This is intended to close review risk around the zero-backend guard and
specialist selection without changing the two round-3 findings into a broader
implementation review.

These are claims to audit, not accepted facts. Look especially for prose versus
machine-registry contradictions, a reviewed row that cannot follow the grammar,
full payloads that cannot be independently reconstructed, circular or
implementation-shaped oracle values, parser-profile bypasses, and governance
stages that fail to bind or protect both oracle files.

## Frozen Bindings

- Reviewed plan SHA-256:
  `25b6c4ac16d8e9dd8c806555ef15732ef6b44ce5517634c12265bfb4ae709718`.
- Reviewed compact oracle SHA-256:
  `d38f3f56ee31716d3eeb729fd036f48708b5cb4d6b71e279f4fdae8f823f8f56`.
- Reviewed materialized oracle SHA-256:
  `ae7aa48fb8c475c7c37c75158a9ed6f83b21a686bc8cd8ca2b28c79b36bcb1ad`.
- Round-3 result SHA-256:
  `75d2017cf31c9747174a66fc8302b2d08925bf46f279b79070e28137398d1f71`.
- P01 stable decision SHA-256:
  `7abc4b00714d0a216aa506cf3308d25a454443eb7316293ea05ec89f3d54a39a`.
- P01 terminal receipt-index SHA-256:
  `5781ab4a7ba23ff865847dd496839a4f827aee78462166606243ad4da591846a`.
- Golden canonical byte count/digest:
  `2375` /
  `f33683e1a14962db2c3713952311df87f34a84b0a75163b21ec1c526b4571d5e`.

Local pre-review checks report:

- valid JSON and clean `git diff --check`;
- 13 source files and 232 supported environments independently located, with
  all obligation/row/line spans matching;
- all 23 reviewed row outcomes matching classifier precedence;
- all 17 normalized targets independently reconstructed from owned source rows;
- all 17 complete payloads/digests/ids reconstructed, with compact/full/source
  projection equality and a byte-exact golden payload;
- exact hashes for 15 source bindings and 20 owned spans;
- all declared regexes compiling, both derived golden ids matching, and 93
  must-change plus five must-not-change mutations passing;
- exact parser executable paths/versions, 13-source allowlist, fixed argv,
  scratch paths, environment, timeouts, and seven-field rubric validated.

Recompute material bindings and a representative cross-section rather than
trusting this statement. Do not run optional exhaustive work if it delays the
material verdict.

## Review Questions

1. Do the repairs close both round-3 blockers without weakening the primary
   exact-field criterion or introducing an implementation-defined choice?
2. Are environment localization, row splitting, classification precedence,
   grouping transitions, conflict handling, reason assignment, and target
   normalization deterministic and mutually consistent with every compact and
   complete oracle record?
3. Does each complete materialized payload follow from exact source bytes plus
   the compact materialization contract, including logical ids, parser fields,
   row indexes/ids, continuation/excluded spans, source-math envelopes,
   provenance, canonical bytes, digest, and id?
4. Are ambiguity/orphan and duplicate-file lookup boundaries represented
   correctly without manufacturing owned bytes or turning lookup ambiguity into
   an obligation identity?
5. Are scanner, normalizer, and identity mutation contracts closed and
   independently executable, including empty identity containers?
6. Can the fixed parser profile compare exact ownership/source fidelity without
   permitting network, recursive corpus scan, executable/environment override,
   scratch escape, proxy promotion, or mathematical-backend execution?
7. Does specialist selection honor the master external-tool-first requirement
   while preventing frozen identity drift after review?
8. Does governance bind, protect, and independently reconstruct the plan,
   compact oracle, materialized oracle, sources, fixtures, parser artifacts,
   invocation ledgers, candidate, reviews, final seal, and stable publication?
9. Can the no-backend guard cover direct imports/calls/subprocesses while
   allowing only the exact parser profile, and can source-edit count and
   allowlist be derived independently?
10. Do fixed commands collect nonzero intended tests, compile every possible
    changed Python path, preserve P00/P01, and avoid backend-executing tests?
11. Do stop and Phase 03 handoff conditions require exact positive equations,
    preserve reviewed adversarial outcomes and proposition-container behavior,
    and retain every non-claim?
12. Identify wrong baselines, proxy promotion criteria, silent defaults, stale
    context, infeasible primitives, unsupported claims, missing mutations, or
    boundary violations.

## Required Output

Findings first, severity ordered, with exact file/line references. Distinguish
material execution blockers from optional improvements. If no material finding
remains, state that explicitly and identify residual implementation/testing
risks without converting them into blockers.

Include exactly one line for each binding below with the actual recomputed
lower-case digest:

```text
Reviewed plan SHA-256: `<lowercase-64-hex>`
Reviewed compact oracle SHA-256: `<lowercase-64-hex>`
Reviewed materialized oracle SHA-256: `<lowercase-64-hex>`
Reviewed bundle SHA-256: `<lowercase-64-hex>`
```

End with exactly one final line:

```text
VERDICT: AGREE
```

or

```text
VERDICT: REVISE
```
