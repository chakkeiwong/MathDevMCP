# Phase 02 Independent Plan Review Round 8 Bundle

Date: 2026-07-11

Review name: `mathdevmcp-real-document-remediation-p02-plan-r8`

Supervisor/executor: Codex

Reviewer: fresh independent Codex read-only reviewer; external Claude transport
is policy-denied, so no content is sent externally

## Role Boundary

READ-ONLY REVIEW ONLY. Bounded local inspection and digest/reconstruction
commands are permitted. Do not edit files, create the formal P02 entry root,
run Phase 02 implementation, execute parser fidelity jobs or mathematical
backends, launch agents, use network/external services, or change repository
state. Do not authorize publication, source edits, Phase 03, or a human,
runtime, product, funding, model-file, or scientific-claim boundary.

## Objective

Determine whether the repaired Phase 02 plan, compact extraction/governance
oracle, complete materialized oracle, and standalone entry bootstrap are
consistent, correct, feasible, deterministic, fail-closed, and complete enough
to implement without inventing entry grammar, snapshot semantics, failure
closure, result binding, identity fields, or expected outputs after
implementation begins.

## Required Artifacts

- `docs/plans/mathdevmcp-real-document-remediation-phase-02-label-scoped-extraction-subplan-2026-07-11.md`;
- `docs/plans/mathdevmcp-real-document-remediation-phase-02-extraction-oracle-2026-07-11.json`;
- `docs/plans/mathdevmcp-real-document-remediation-phase-02-materialized-obligations-oracle-2026-07-11.json`;
- `docs/plans/p02_entry_bootstrap_20260711.py`;
- `docs/reviews/mathdevmcp-real-document-remediation-phase-02-plan-review-r7-result-2026-07-11.md`;
- earlier Phase 02 review results where needed to assess regression;
- exact reviewed fixtures, two frozen sources, sealed P00/P01 evidence trees,
  and relevant current code only where needed for feasibility.

Do not broaden into Phase 02 implementation or Phase 03 semantics.

## Round 7 Findings And Claimed Repairs

Round 7 returned `REVISE` with three material bootstrap-contract defects. The
current bytes claim these visible repairs:

1. Review-budget closure: the plan, oracle, and executable accept only the
   exact R8 result ref for these bytes. R7, R9, R10+, arbitrary, glob, latest,
   repeated, abbreviated, equal-sign, positional, missing, or extra argument
   forms fail before workspace/root/file work. If R8 returns `REVISE`, any R9
   candidate must visibly advance the selector to R9 and undergo the final
   authorized review before entry bootstrap.
2. Snapshot postcondition: after writing, the bootstrap requires the Phase 02
   tree to contain exactly one `entry` directory and four regular outputs,
   reopens all four outputs no-follow, strictly parses each manifest,
   re-enumerates the complete implementation/protected/immutable scopes,
   reopens every current input no-follow, reconstructs all three manifests from
   current membership and bytes, and requires byte equality. Only the four
   separately verified bootstrap outputs are excluded from the second dirty
   enumeration; any other added/removed/changed input fails.
3. No-retry closure: any pre-existing Phase 02 phase root is rejected before
   loading the oracle or reading snapshot inputs. Phase and entry directories
   are created by no-follow directory-descriptor traversal. A failure after the
   phase-root mkdir preserves partial state; every later invocation rejects the
   preserved phase root, including the phase-root-only case.

Adjacent hardening added before R8:

- The only permitted process uses the pinned Python with exact `-B -S`, exact
  `sys.orig_argv`, and an exact six-key clean environment. Site initialization,
  bytecode writes, extra environment keys, missing/reordered flags, and source/
  profile disagreement on external argv, Git argv, environment, or write order
  fail before mkdir.
- Workspace `.git`, source/test/script/review roots, and local-evidence
  ancestors must be real non-symlink directories.
- Before mkdir, the bootstrap derives the 13 unique source refs/digests from
  the fixture/frozen cases, requires exact set equality with the parser source
  allowlist, verifies every source digest, validates every environment, label,
  owned, and excluded interval, and rehashes every declared owned span.

These are claims to audit, not accepted facts. Look especially for a review
selector outside R8, a Python argv/environment/site escape, a source/profile
constant mismatch, a stale manifest that still passes after byte or membership
drift, bootstrap-output self-observation, a fifth phase-tree output, a phase-
root-only retry, a symlink/ancestor escape, an incomplete source projection, or
a post-write success path that does not verify the bytes it reports.

## Frozen Bindings

- Reviewed plan SHA-256:
  `41f27b1b78685f062128fafbe49b76c624eda813b6d3a919899a8a59b9041fae`.
- Reviewed compact oracle SHA-256:
  `6796117b886d6a8a1d4a79e156e6b9121810c291b3d41f44a0174ffaa76567da`.
- Reviewed materialized oracle SHA-256:
  `ae7aa48fb8c475c7c37c75158a9ed6f83b21a686bc8cd8ca2b28c79b36bcb1ad`.
- Reviewed entry bootstrap SHA-256:
  `b54c654cd1ac2f8ad3ab4f1c5f2146287e28f179f79a956f31af3978ea403adb`.
- Round-7 result SHA-256:
  `a68e7121f545ad7ea9b12c9b6b9e8999b53bb80044869738ba207ea243078506`.
- P01 stable decision SHA-256:
  `7abc4b00714d0a216aa506cf3308d25a454443eb7316293ea05ec89f3d54a39a`.
- P01 terminal receipt-index SHA-256:
  `5781ab4a7ba23ff865847dd496839a4f827aee78462166606243ad4da591846a`.
- Golden canonical byte count/digest:
  `2375` /
  `f33683e1a14962db2c3713952311df87f34a84b0a75163b21ec1c526b4571d5e`.

## Local Pre-Review Evidence

- Skeptical audit passed for plan review: the measured contamination remains
  the baseline; exact bytes/spans/identities remain primary; parser success and
  counts remain non-promotional; publication, backend execution, source edits,
  and Phase 03 remain prohibited; failure/review/post-link/chain stop conditions
  remain explicit.
- Bootstrap compilation, strict parsing of both JSON files, `git diff --check`,
  and absence of the formal `.local/mathdevmcp/evidence/p02-20260711` root
  passed.
- Independent source materialization reproduced all 17 complete obligations
  and unchanged materialized-oracle SHA-256 `ae7aa48...`.
- Combined contract audit passed: 26 actions, nine subprocess and 17 native;
  20 unique transitions; 22 compile paths; exact 22/33/57-key schemas; nine
  nullable pairs; six representative terminal traces; 17 unique identities;
  golden bytes 2375; 93 must-change and five must-not-change mutations; 13
  parser sources, seven fidelity fields, and both multi-label ambiguity routes.
- In fresh disposable mirrors, the exact hardened R8 invocation succeeded,
  wrote exactly four outputs with 15 immutable, 285 implementation, and 536
  protected manifest records, and a second exact invocation failed with output
  digests unchanged.
- R7, R9, R10, repeated, equal-sign, abbreviated, positional, extra, missing,
  extra-environment, missing-`-S`, and symlinked-evidence-ancestor forms failed
  before Phase 02 writes.
- Controlled disposable fault injection showed that implementation membership
  addition and already-listed byte drift after the fourth write both fail with
  the four-file partial root preserved; failure between phase-root and entry-
  root mkdir leaves only the phase root; each subsequent uninstrumented run
  rejects the preserved phase root. The reviewed bootstrap itself was not
  modified for those injected runs.

Recompute material bindings and a representative cross-section rather than
trusting these statements. Do not create formal entry evidence.

## Review Questions

1. Do the exact-R8 selector and documented R9 advancement rule close the review-
   budget escape without permitting skipped or post-budget entry creation?
2. Does argv validation reject every alternate Python/option form before root
   or file work while accepting the exact profiled invocation?
3. Do executable constants and compact profile agree exactly on external argv,
   clean environment, Git argv, output refs, and write order?
4. Do `-B -S`, exact environment, exact `sys.orig_argv`, and root validation
   eliminate site, bytecode, caller environment, and symlink-ancestor influence
   within the stated local trust boundary?
5. Does the pre-mkdir source audit cover all 13 unique fixtures/frozen sources,
   source digests, environment/label/owned/excluded intervals, and owned-span
   digests without silently selecting implementation output?
6. Does post-write scope re-enumeration detect byte drift, deletion, addition,
   rename, dirty-state change, review addition, evidence-tree mutation, or a
   fifth Phase 02 output while correctly excluding only the four bootstrap
   outputs from self-observation?
7. Can any success be reported without reopening the exact four outputs and
   reconstructing all three current manifests, or can manifest parsing accept
   duplicate, unsorted, malformed, non-UTF-8, non-canonical, or special refs?
8. Does no-follow directory creation plus pre-existing phase-root rejection
   close every partial-mkdir/no-retry path without deleting or overwriting
   evidence?
9. Does the entry bootstrap remain non-circular while binding its exact source,
   agreeing review, all pre-implementation bytes, protected state, immutable
   inputs, and sealed predecessor evidence?
10. Does the governance failure suffix still close every reachable nonzero
    action and verified `REVISE` with a unique successor and no path back to
    candidate/publication?
11. Are source trigger, pre-repair, pre-close, and terminal-close indices,
    nullable stages, verdicts, logs, vetoes, non-claims, and repairs fully
    reconstructible without narrative or boolean authority?
12. Is post-link publication failure fail-closed without retry, deletion,
    fabricated close, or false pass?
13. Are all action execution classes, handler/child nullability, argv,
    environment, artifact-ref grammar, binding keys, compile paths, fixed tests,
    allowlist, and protected-state checks mutually consistent and feasible?
14. Do all 17 compact/materialized/source identities, grouping routes, parser-
    fidelity boundaries, mutation contracts, zero-backend/source rules, and
    publication quarantine remain closed and unchanged?
15. Do stop/handoff conditions retain every non-claim and prohibit Phase 03
    from a revised, closed, chain-failed, unaudited, or unpublished P02 round?
16. Identify wrong baselines, proxy promotion criteria, silent defaults, stale
    context, environment mismatches, infeasible primitives, unsupported claims,
    missing mutations, or boundary violations.

## Required Output

Findings first, severity ordered, with exact file/line references. Distinguish
material execution blockers from optional improvements. If no material finding
remains, state that explicitly and identify residual implementation/testing
risks without converting them into blockers.

Include exactly one line for each binding below with the actual recomputed
lower-case digest:

```text
Reviewed plan SHA-256: `<lowercase-64-hex>`
Reviewed compact oracle SHA-256: `<lowercase-64-hex>`
Reviewed materialized oracle SHA-256: `<lowercase-64-hex>`
Reviewed entry bootstrap SHA-256: `<lowercase-64-hex>`
Reviewed bundle SHA-256: `<lowercase-64-hex>`
```

End with exactly one final line:

```text
VERDICT: AGREE
```

or

```text
VERDICT: REVISE
```
