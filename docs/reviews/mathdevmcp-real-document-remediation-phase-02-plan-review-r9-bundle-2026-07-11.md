# Phase 02 Independent Plan Review Round 9 Bundle

Date: 2026-07-11

Review name: `mathdevmcp-real-document-remediation-p02-plan-r9`

Supervisor/executor: Codex

Reviewer: fresh independent Codex read-only reviewer; external Claude transport
is policy-denied, so no content is sent externally

Review budget: final authorized substantive Phase 02 plan-review round. An
`AGREE` verdict permits the exact reviewed entry bootstrap. A `REVISE` verdict
exhausts the authorized R5-R9 budget and requires a blocker record plus human
direction; it does not permit R10, formal entry creation, or implementation.

## Role Boundary

READ-ONLY REVIEW ONLY. Bounded local inspection and digest/reconstruction
commands are permitted. Do not edit files, create the formal P02 entry root,
run Phase 02 implementation, execute parser fidelity jobs or mathematical
backends, launch agents, use network/external services, or change repository
state. Do not authorize publication, source edits, Phase 03, or a human,
runtime, product, funding, model-file, or scientific-claim boundary.

## Objective

Determine whether the repaired Phase 02 plan, compact extraction/governance
oracle, complete materialized oracle, and standalone entry bootstrap are
consistent, correct, feasible, deterministic, fail-closed, and complete enough
to implement without inventing entry grammar, source/environment projections,
snapshot semantics, failure closure, result binding, identity fields, or
expected outputs after implementation begins.

## Required Artifacts

- `docs/plans/mathdevmcp-real-document-remediation-phase-02-label-scoped-extraction-subplan-2026-07-11.md`;
- `docs/plans/mathdevmcp-real-document-remediation-phase-02-extraction-oracle-2026-07-11.json`;
- `docs/plans/mathdevmcp-real-document-remediation-phase-02-materialized-obligations-oracle-2026-07-11.json`;
- `docs/plans/p02_entry_bootstrap_20260711.py`;
- `docs/reviews/mathdevmcp-real-document-remediation-phase-02-plan-review-r8-result-2026-07-11.md`;
- earlier Phase 02 review results where needed to assess regression;
- exact reviewed fixtures, two frozen sources, sealed P00/P01 evidence trees,
  and relevant current code only where needed for feasibility.

Do not broaden into Phase 02 implementation or Phase 03 semantics.

## Round 8 Finding And Claimed Repair

Round 8 returned `REVISE` with one material defect. The pre-mkdir source audit
validated only an outer `environment_span`, dropped the inherited
`nested_environment_span`, and did not independently relocate and pair exact
comment-aware begin/end environment descriptors. The current bytes claim this
visible repair:

1. The selector is final-budget exact R9. R8, R10+, skipped, arbitrary, glob,
   latest, repeated, abbreviated, equal-sign, positional, missing, and extra
   forms remain forbidden before root/file work.
2. Every reviewed source is strict UTF-8 decoded. Unescaped comments are masked
   using the exact even-consecutive-backslash rule. Every supported environment
   token is located from source bytes, paired by exact captured name using a
   stack, and crossed or unclosed supported environments are rejected.
3. For each of the 17 compact obligation paths, the bootstrap inherits the
   outer `environment_span` and `nested_environment_span` when present,
   requires this ordered outer-to-selected chain to equal the independently
   source-derived supported-environment ancestry, and verifies strict nesting.
4. The source-derived selected descriptor is reconstructed from the matched
   begin token. Exact label token/value and all label, owned, and excluded spans
   must lie in the final selected environment; owned/excluded sets are sorted,
   unique, disjoint, and every owned-span digest is recomputed.
5. The bootstrap strict-loads all 17 materialized records, verifies their exact
   compact path set and canonical byte counts/digests/ids, reconstructs each
   environment-stack descriptor and canonical environment id from source,
   reconstructs the selected descriptor/span/line bounds, and requires exact
   equality with the materialized identity environment plus document source
   ref/digest and label.

The R7 repairs remain in force: exact-current-round review selection,
post-write complete scope reconstruction, and phase-root-only no-retry. Exact
`-B -S` runtime/environment/profile agreement, no-follow ancestor traversal,
13-source allowlist equality, source digests, protected-state binding, and
four-output tree closure also remain in force.

These are claims to audit, not accepted facts. Look especially for a comment-
mask escape, unsupported/supported nesting interaction, crossed-name pairing
that passes, dropped inherited nested span, an ancestry chain accepted out of
order, a begin descriptor not bound to its matching end, selected-versus-outer
containment confusion, environment-id construction drift, line-bound drift,
materialized-path omission/duplication, a canonical-identity bypass, or a new
infeasible/circular dependency.

## Frozen Bindings

- Reviewed plan SHA-256:
  `3f9cb7ce3c70bdb2b06f41a1ec1510658044c3a3f33acb1593005c2ca2c7c2c8`.
- Reviewed compact oracle SHA-256:
  `3b5792cd82992402e58b6826d6d9b897fa097a7d369e540053179c6a9b910b1c`.
- Reviewed materialized oracle SHA-256:
  `ae7aa48fb8c475c7c37c75158a9ed6f83b21a686bc8cd8ca2b28c79b36bcb1ad`.
- Reviewed entry bootstrap SHA-256:
  `6fe84454491fe76e52daf789292ea4a44d695acb244fcdcf78838356bf2b55ec`.
- Round-8 result SHA-256:
  `3f96dc28428bf33962b27e56301b5d70514ff443910868ababd8bf89e9af6994`.
- P01 stable decision SHA-256:
  `7abc4b00714d0a216aa506cf3308d25a454443eb7316293ea05ec89f3d54a39a`.
- P01 terminal receipt-index SHA-256:
  `5781ab4a7ba23ff865847dd496839a4f827aee78462166606243ad4da591846a`.
- Golden canonical byte count/digest:
  `2375` /
  `f33683e1a14962db2c3713952311df87f34a84b0a75163b21ec1c526b4571d5e`.

## Local Pre-Review Evidence

- Skeptical audit passed for plan review: measured cross-label contamination
  remains the baseline; exact bytes/spans/identities remain primary; parser
  success/counts remain non-promotional; publication, backend execution, source
  edits, and Phase 03 remain prohibited; all stop conditions remain explicit.
- Bootstrap compilation, strict parsing of both JSON files, `git diff --check`,
  and absence of the formal P02 root passed.
- Independent source materialization reproduced all 17 complete obligations
  and unchanged materialized-oracle SHA-256 `ae7aa48...`.
- Combined contract audit passed: 26 actions, nine subprocess and 17 native;
  20 unique transitions; 22 compile paths; 22/33/57-key schemas; nine nullable
  pairs; six terminal traces; 17 unique identities; golden bytes 2375; 93/5
  identity mutations; 13 parser sources; seven fidelity fields; both multi-
  label ambiguity routes.
- In a fresh disposable mirror, the exact hardened R9 bootstrap succeeded only
  after the full 13-source and 17-materialized-record cross-check and wrote the
  expected four entry files. The formal repository root remained absent.
- The nested aligned fixture independently reconstructed source-derived chain
  `[(0,105), (17,90)]` and descriptor `aligned`, unstarred. Commented fake
  environment tokens were masked. Wrong nested endpoint/order, wrong
  materialized descriptor, wrong materialized environment id, and wrong
  materialized line bound were each rejected by the reviewed functions.
- Prior fault tests for byte/membership drift, between-mkdir failure, retry,
  malformed argv/environment, review-budget escape, fifth output, and symlinked
  evidence ancestor remain applicable; the corresponding code/profile bytes
  were preserved except for the bounded R8 environment-projection repair.

Recompute material bindings and a representative cross-section rather than
trusting these statements. Do not create formal entry evidence.

## Review Questions

1. Does exact-R9 selection fully enforce the human budget, including a hard
   stop after `REVISE`, with no R10 or skipped-round route?
2. Does the source parser implement the exact comment rule and supported-token
   regex, pair exact names correctly, reject crossed/unclosed stacks, and
   derive correct supported ancestry in the presence of unsupported nesting?
3. Is inherited `nested_environment_span` preserved for every applicable item,
   and must declared outer-to-selected spans equal source-derived ancestry in
   exact order rather than mere containment?
4. Are begin/end descriptors, kind/starred state, selected span, and strict
   nesting all reconstructed from source rather than trusted from the oracle?
5. Are label token/value, owned/excluded spans, ownership of the label, span
   ordering/disjointness, and owned-span hashes checked against the final
   selected environment rather than an outer container?
6. Are canonical environment-stack IDs computed with exact P01 canonical JSON
   semantics and compared, along with descriptor/span/line fields, against all
   17 materialized identity environments?
7. Are all 17 materialized paths present exactly once with strict record keys,
   canonical byte count, digest, id, document source ref/digest, and label, with
   no circular dependence on implementation output?
8. Do the source, compact, materialized, parser allowlist, and bootstrap
   projections form one exact closed chain, including duplicate-file and
   ambiguous/orphan cases?
9. Do all R7 repairs remain closed: post-write complete scope reconstruction,
   four-output self-observation, byte/membership drift detection, partial-root
   preservation, and no retry?
10. Are exact process argv/environment/runtime flags, source/profile constants,
    no-follow traversal, input regularity, and workspace ancestor checks
    mutually consistent and feasible?
11. Does the entry bootstrap remain non-circular while binding its own exact
    source, R9 review, pre-implementation bytes, immutable inputs, protected
    state, and sealed predecessor evidence?
12. Does the governance failure suffix still close every nonzero action and
    verified `REVISE`, including correct source/pre-repair/pre-close/terminal
    indices, with no path back to candidate or publication?
13. Are nullable stages, reviews, audits, logs, vetoes, non-claims, repairs,
    result reconstruction, and post-link failure all fail-closed without prose
    or boolean authority?
14. Are action classes, argv/environments, artifact refs, bindings, compile
    paths, fixed tests, implementation allowlist, protected state, zero-backend
    guard, and publication quarantine consistent and feasible?
15. Do all extraction/grouping/parser/mutation contracts and Phase 03 stop/
    handoff boundaries remain closed and unchanged after the repair?
16. Identify wrong baselines, proxy promotion criteria, silent defaults, stale
    context, environment mismatches, infeasible primitives, unsupported claims,
    missing mutations, or boundary violations.

## Required Output

Findings first, severity ordered, with exact file/line references. Distinguish
material execution blockers from optional improvements. If no material finding
remains, state that explicitly and identify residual implementation/testing
risks without converting them into blockers.

Include exactly one line for each binding below with the actual recomputed
lower-case digest:

```text
Reviewed plan SHA-256: `<lowercase-64-hex>`
Reviewed compact oracle SHA-256: `<lowercase-64-hex>`
Reviewed materialized oracle SHA-256: `<lowercase-64-hex>`
Reviewed entry bootstrap SHA-256: `<lowercase-64-hex>`
Reviewed bundle SHA-256: `<lowercase-64-hex>`
```

End with exactly one final line:

```text
VERDICT: AGREE
```

or

```text
VERDICT: REVISE
```
