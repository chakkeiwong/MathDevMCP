# Phase 03 Plan Review R1 Compact Aid

This compact aid is not a plan amendment. The reviewed full plan remains
`docs/plans/mathdevmcp-real-document-remediation-phase-03-semantic-resolution-and-corpus-context-subplan-2026-07-12.md`
at SHA-256
`e81a200337a2c6f98324c6d5d16904188cf860c25cb828030141d16cbe4d5a70`.
The reviewed bootstrap remains `docs/plans/p03_entry_bootstrap_20260712.py` at
SHA-256
`adf24dacbdc7605ce4da729694fa8407ee865cc026359b80d331d23d34b63b74`.

## Objective And Primary Contract

Replace directory-wide sibling indexing and local lexical status promotion with
entry-rooted dependency traversal, exact provenance, ambiguity-preserving
symbol/notation resolution, orthogonal assumption support/encoding states, and
separate parser/context/mathematical/engineering/interpretation ledgers.

The inherited P02 bundle has exactly 17 ordered unique obligations: 14
`valid_complete`/adapter-eligible, two `ambiguous`/ineligible, and one
`orphaned`/ineligible. Exactly 14 receive bounded context-search manifests.
Exactly three receive extraction-veto manifests with zero semantic traversal,
candidates, or support claims. All 17 digests must be covered exactly once.

Source support requires entry reachability, exact file digest and non-null
byte/line span, a dependency path, and applicability evidence. Lexical
proximity is candidate evidence only. `not_found_after_search` requires the
complete predeclared reachable closure and no relevant engineering diagnostic;
otherwise the state is `not_searched`.

`\pi` in the frozen card document is policy or ambiguous from source evidence,
never posterior by spelling. Candidate assumptions cannot become stated/source
supported without the normative support transition. Report output must not
collapse context or engineering failures into mathematical gaps.

## Corpus And Backend Boundaries

The new graph accepts an exact entry ref and declared corpus root. Only explicit
braced `\input`/`\include` edges inside that root create reachability; sharing a
directory never does. Missing/cyclic/unsupported includes are typed engineering
diagnostics; symlink and out-of-root edges are integrity vetoes. Existing
directory-wide `build_index(root)` remains for legacy diagnostics, but P03 must
fail closed without an entry ref.

P03 formal evidence imports only a new backend-free `context_evidence` route.
The legacy `audit_document_derivation_tree` route, doctor, controllers,
formalizers, compilers, mathematical backends, network, GPU, installers, source
edits, publication, and Phase 04 execution are forbidden. A no-backend guard and
manifest-before-import test enforce this boundary.

## Governance And Entry

Success actions are the exact ordered 24-item tuple bound in the bootstrap,
ending result-review binding, final candidate, separate final-seal audit, and
disabled stable publication. Failure-only suffix is exactly
`bind_scoped_repair`, `close_round`. Receipts/indexes are append-only and
no-overwrite. Failed terminal sealing is human recovery, never retry.

The bootstrap preflight independently reopens P02 fixed digests, stable/final
hard-link identity, candidate validation, exact 24-receipt pass chain, terminal
same-inode/same-digest, bundle semantic digest, zero backend/source edits, all
17 ordered obligation digest/state/eligibility bindings, the exact 14/2/1
counts, implementation allowlist, action grammar, and absence of the P03 root.
Its last observed output was `PASS_NO_WRITE`.

Create additionally requires one fixed uniquely agreeing plan review and strict
canonical budget JSON reserving exactly one result review and one separate
final-seal audit. It records the reviewed artifacts, P02 refs, ordered
obligation bindings, action registry, implementation/protected/immutable
manifests, allowlist, CPU-only mode, and disabled publication, then allocates the
four-file entry once with no-replace writes. Any existing/partial P03 root is a
stop.

Frozen source digests are reopened through `EXPECTED_DIGESTS` and included in
the immutable-input manifest; the current entry record does not duplicate them
as a direct map. Runtime fields currently record the exact measured local values
Python `3.11.15` and pytest `9.0.2`; executable identity and Python flags are
checked, while pytest version is not programmatically remeasured inside the
bootstrap.

## Items Requiring Verdict

1. Is the stale required test name
   `test_every_inherited_obligation_has_exactly_one_search_manifest` materially
   ambiguous given the controlling 14-search plus 3-extraction-veto contract?
2. Must frozen digests be duplicated directly in the entry record, or is their
   verified inclusion in the immutable manifest sufficient?
3. Must Python/pytest versions be programmatically measured in the bootstrap,
   or are the exact executable check and immutable source/environment snapshot
   sufficient at entry?
4. Is any other material baseline, provenance, state, backend, governance,
   artifact, review-budget, or stop-condition defect apparent?

For exact predecessor bindings and required output grammar, use
`docs/reviews/mathdevmcp-real-document-remediation-phase-03-plan-review-r1-bundle-2026-07-12.md`.
