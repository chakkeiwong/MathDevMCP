# Source-Bound Semantic Repair Plan Review Bundle

Date: 2026-07-16

Role: read-only plan reviewer. Codex remains supervisor and executor.

## Objective

Review whether
`docs/plans/mathdevmcp-source-bound-semantic-repair-and-reaudit-plan-2026-07-16.md`
is scientifically sound, feasible, and safe to execute against the concrete
gaps in
`docs/plans/mathdevmcp-credit-card-full-mission-audit-result-2026-07-16.md`.

Do not edit files, execute commands, authorize publication, or review the whole
repository.

## Critical Context

- The source is a 7,566-line applied mathematical proposal with definitions,
  identities, empirical claims, and policy constructs.
- Four generic workflows falsely reported a source terminal-value definition as
  a refuted free-variable theorem.
- The repaired plan does not infer definitions from equality syntax and now
  distinguishes `source_evidenced_role`, `caller_asserted_role`, and
  `role_ambiguous`. Only a role validated against an exact source span plus
  file/content identity may alter proof routing. Caller assertions remain
  diagnostic, while no-role generic equalities retain theorem/counterexample
  behavior.
- The repaired document-tree extractor is already correct for the selected
  multiline rows. The plan makes other workflows consume that canonical
  label-scoped obligation rather than creating another extractor.
- The exact public-tool audit is repeated after repair in two views: a frozen
  like-for-like comparison over the original 57 names, and a separate delta
  inventory for current-registry additions/removals/schema changes. Additive
  role and file-identity fields are disclosed as repaired contract inputs.
- A valuation template/backend pass may establish only scoped algebraic
  consistency under an explicit definition and denominator domain, never
  economic validity, causal identification, policy optimality, or
  whole-document proof.

## Evidence Contract

The primary criterion is closure of the source-role, extraction, ambiguity,
assumption-discharge, and one bounded valuation-execution gaps without
weakening theorem counterexamples or claim boundaries. Test counts, blocker
counts, payload bytes, and runtime are explanatory only.

Hard vetoes include:

- definition inferred from syntax alone;
- valid theorem counterexample suppressed;
- caller role promoted without source binding;
- silent first-match label lookup;
- incomplete multiline target;
- CAS/template result promoted to economic or mathematical proof;
- source mutation or publication enablement;
- repeat audit made easier without disclosure.

## Review Questions

1. Do the repaired source-evidenced/caller-asserted/ambiguous role authorities
   close the routing-authority gap without weakening no-role theorem behavior?
2. Is the phase order feasible, or is there a dependency that should move?
3. Does the frozen original-57 comparison plus separate current-registry delta
   keep the re-audit fair despite additive role/file inputs and new tools?
4. Are the valuation-template and backend claims sufficiently bounded?
5. Are any material assumptions, stop conditions, public-surface effects, or
   test obligations missing?
6. Does any phase overreach relative to the concrete audit gaps?

## Required Response

List material findings first, with the relevant plan section and a concrete
repair. Ignore stylistic preferences. End with exactly one line:

```text
VERDICT: AGREE
```

or

```text
VERDICT: REVISE
```
